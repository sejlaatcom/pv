# على الاستضافات التي لا تسمح بتغيير مجلد الجذر: وجّه كل الطلبات إلى مجلد public
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
